# Railway / Nixpacks build config
# The Node.js app lives in backend/ — this tells Nixpacks where to find it.

[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
dependsOn = ["setup"]
cmds = ["npm install --prefix backend"]

[start]
cmd = "node backend/server.js"
